h1 Listing settings

table
  thead
    tr
      th Name
      th
      th
      th

  tbody
    - @settings.each do |setting|
      tr
        td = setting.name
        td = link_to 'Show', setting
        td = link_to 'Edit', edit_setting_path(setting)
        td = link_to 'Destroy', setting, data: {:confirm => 'Are you sure?'}, :method => :delete

br

= link_to 'New Setting', new_setting_path

br
hr

h1 Listing Store settings

table
  thead
    tr
      th Store Name
      th Name

  tbody
    - @store_settings.each do |setting|
      tr
        td = setting.store_name
        td = setting.name
br
